const DEFAULT_ROLE = 'R3'
const ADMIN_ROLE = 'R1'
const DEFAULT_LIMIT = 16
const DEFAULT_PAGE_SIZE = 1
const MESSAGE_WELCOME = 'Chào bạn ! mình có thể giúp gì cho bạn.' 

module.exports = {
  DEFAULT_ROLE,
  ADMIN_ROLE,
  DEFAULT_LIMIT,
  DEFAULT_PAGE_SIZE,
  MESSAGE_WELCOME
}